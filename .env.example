# Database (porta não exposta externamente)
POSTGRES_USER=seaf
POSTGRES_PASSWORD=sua_senha_aqui
POSTGRES_DB=seaf

# Backend (interno)
NODE_ENV=development
CORS_ORIGIN=*

# Frontend (variáveis de build)
VITE_API_URL=http://localhost
VITE_ACCESS_PASSWORD=sua_senha_de_acesso
